name: test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon

      - name: Move and rename APK
        run: |
          mkdir -p out
          cp app/build/outputs/apk/debug/*.apk out/music-tag-repair-debug.apk

      - name: Publish prerelease
        uses: softprops/action-gh-release@v2
        with:
          tag_name: test
          name: music-tag-repair test
          prerelease: true
          make_latest: false
          files: out/music-tag-repair-debug.apk
